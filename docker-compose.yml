version: "3.8"

services:
  neo4j:
    image: neo4j:5.26-community
    ports:
      - "7474:7474"
      - "7687:7687"
    environment:
      NEO4J_AUTH: neo4j/reboot_dev
    volumes:
      - neo4j_data:/data

volumes:
  neo4j_data:
